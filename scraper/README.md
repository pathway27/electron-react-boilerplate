# Scraper that fetches game info. for our db

## Development

```bash
docker-compose run --rm scrapy
scrapy shell "url"
# or star hacking the spiders
# winpty docker exec -it <container_id> sh # windows git bash
```
